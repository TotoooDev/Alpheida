#include <config.h>

#ifdef SHRIMP_WII

#include <texture.h>

typedef struct Texture {

} Texture;

Texture* texture_new(const char* filename) {

}

void texture_delete(Texture* texture) {
    
}

#endif
